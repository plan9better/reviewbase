name: sample-action

on:
  workflow_call:
    inputs:
      text:
        required: true
        type: string
jobs:
  auto-deploy:
    name: Auto-deploy
    runs-on: ubuntu-latest
    steps: 
      - id: echo
        name: echo
        shell: bash
        run: |
          echo ${{inputs.text}}
